version: "3.9"

services:
  contentpipe:
    build: .
    container_name: contentpipe
    restart: unless-stopped
    ports:
      - "8765:8765"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./output:/app/output
